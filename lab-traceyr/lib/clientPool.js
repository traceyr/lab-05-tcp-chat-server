const EE = require('events');
const net = require('net')l

let events = new EE();
function ClientPool(ee, pool){
  this.ee = new EE();
  this.pool = {};
}

ee.on('register', () =>{

})

//server.js folder
var server = net.createServer(function(socket){
  ee.emit('register');
});
